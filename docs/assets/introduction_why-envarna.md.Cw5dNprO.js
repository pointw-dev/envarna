import{_ as a,c as i,o as t,al as s}from"./chunks/framework.DYoXd6M_.js";const u=JSON.parse('{"title":"Why envarna?","description":"","frontmatter":{"head":[["link",{"rel":"canonical","href":"https://pointw-dev.github.io/envarna/introduction/why-envarna.html"}]]},"headers":[],"relativePath":"introduction/why-envarna.md","filePath":"introduction/why-envarna.md"}'),n={name:"introduction/why-envarna.md"};function o(r,e,l,d,c,h){return t(),i("div",null,e[0]||(e[0]=[s('<h1 id="why-envarna" tabindex="-1">Why envarna? <a class="header-anchor" href="#why-envarna" aria-label="Permalink to &quot;Why envarna?&quot;">​</a></h1><blockquote><p>Why not just use <code>process.env</code>? Isn’t it good enough?</p></blockquote><p><code>dotenv</code> and <code>process.env</code> are time-tested, widely used tools. Envarna builds on them. For many projects, they’re all you need. But as configuration grows more complex, envarna steps in to provide structure, validation, and safety on top of that foundation.</p><p>Here’s how envarna complements what <code>process.env</code> already does well, and why it can be worth adopting even in projects that start small.</p><h2 id="everyone-understands-process-env-why-introduce-decorators-and-a-new-library" tabindex="-1">Everyone understands <code>process.env</code>. Why introduce decorators and a new library? <a class="header-anchor" href="#everyone-understands-process-env-why-introduce-decorators-and-a-new-library" aria-label="Permalink to &quot;Everyone understands `process.env`. Why introduce decorators and a new library?&quot;">​</a></h2><p>Exactly! <code>process.env</code> is familiar and universal. That’s one reason envarna works <em>with</em> it, not against it. But as your configuration surface area grows, the risk of errors grows too:</p><ul><li>Misspelled or reused variables</li><li>Scattered defaults and fallback logic</li><li>Type coercion bugs</li><li>Latent failures that surface only at runtime</li></ul><p>Envarna adds structure without taking away control. You define what you expect, with types and defaults, in one place. Decorators give you lightweight structure that scales with your needs.</p><h2 id="it-s-simple-just-do-process-env-foo-bar-and-move-on" tabindex="-1">It’s simple. Just do <code>process.env.FOO || &#39;bar&#39;</code> and move on. <a class="header-anchor" href="#it-s-simple-just-do-process-env-foo-bar-and-move-on" aria-label="Permalink to &quot;It’s simple. Just do `process.env.FOO || &#39;bar&#39;` and move on.&quot;">​</a></h2><p>This works—until:</p><ul><li>You forget to coerce a value (<code>parseInt</code>, <code>=== &quot;true&quot;</code>, etc.)</li><li>You apply different defaults in different files</li><li>You forget to validate the format</li></ul><p>With envarna, defaults, coercion, and validation are expressed declaratively:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">settings</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(v.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">positive</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">maxPageSize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: number;</span></span></code></pre></div><p>One source of truth, and it&#39;s enforced automatically.</p><h2 id="this-is-fine-for-small-apps-i-don-t-need-a-framework-just-for-config" tabindex="-1">This is fine for small apps. I don’t need a framework just for config. <a class="header-anchor" href="#this-is-fine-for-small-apps-i-don-t-need-a-framework-just-for-config" aria-label="Permalink to &quot;This is fine for small apps. I don’t need a framework just for config.&quot;">​</a></h2><p>That may be true, configuration complexity tends to creep up:</p><ul><li>A second environment (staging)</li><li>A few conditional flags</li><li>A new team member unsure which env vars are required</li></ul><p>Envarna stays lightweight but gives you room to grow:</p><ul><li>Logical grouping of settings (e.g. SMTP, DB, PubSub)</li><li>CLI-based discovery (<code>npx envarna list</code>, <code>env</code>, etc.)</li><li>Code completion and documentation via decorators</li></ul><h2 id="at-least-with-process-env-everything-is-in-one-place-i-can-see-the-behavior-right-there" tabindex="-1">At least with <code>process.env</code>, everything is in one place. I can see the behavior right there. <a class="header-anchor" href="#at-least-with-process-env-everything-is-in-one-place-i-can-see-the-behavior-right-there" aria-label="Permalink to &quot;At least with `process.env`, everything is in one place. I can see the behavior right there.&quot;">​</a></h2><p>It can feel that way, but in practice the responsibility for handling configuration (parsing, coercion, validation, and defaulting) often gets mixed into unrelated parts of the codebase:</p><ul><li>Conditionals buried inside functions</li><li>Defaults embedded in constructors</li><li>Validation scattered across utility files</li></ul><p>This tight coupling makes it harder to change or audit configuration logic, and it violates separation of concerns.</p><p>With envarna:</p><ul><li>Type, default, and validation are co-located with the declaration of each setting</li><li>Application logic uses already-validated values and doesn’t worry about how they were derived</li><li>Other complex validation is also part of the settings class: e.g. mutually-exclusive settings, settings that must be set together (e.g. username/password).</li></ul><p>It preserves the benefits of a single source and moves that source to a clearly defined, purpose-built layer.</p><h2 id="testing-with-process-env-is-fine-if-you-re-careful" tabindex="-1">Testing with <code>process.env</code> is fine if you’re careful. <a class="header-anchor" href="#testing-with-process-env-is-fine-if-you-re-careful" aria-label="Permalink to &quot;Testing with `process.env` is fine if you’re careful.&quot;">​</a></h2><p>It can work, but requires discipline:</p><ul><li>Set the env var before the module is imported</li><li>Clean it up after the test</li><li>Reload modules if env values change</li></ul><p>Which means it&#39;s error-prone.</p><p>Envarna makes it safer:</p><ul><li>Inject values directly via <code>.load({ field: value })</code></li><li>Or mock a scoped portion of the <code>settings</code> object</li><li>No global mutation, no test bleed-through</li></ul><p>Your tests stay readable, predictable, and isolated.</p><h2 id="this-looks-like-overkill-for-10–20-variables" tabindex="-1">This looks like overkill for 10–20 variables. <a class="header-anchor" href="#this-looks-like-overkill-for-10–20-variables" aria-label="Permalink to &quot;This looks like overkill for 10–20 variables.&quot;">​</a></h2><p>Maybe. But even with 10 variables, you often end up with:</p><ul><li>Fallbacks scattered across files</li><li>Missing or misnamed variables</li><li>Unintentional reuse or inconsistent defaults</li></ul><p>With envarna:</p><ul><li>Definitions are centralized</li><li>Validation is enforced early</li><li>Developer experience improves with type safety and code completion</li></ul><p>You don’t need to reach for envarna—but if you do, it’ll meet you where you are and grow with you.</p><hr><p>Still unsure? Try envarna in a single feature module. It may feel like a small shift now, but it prevents the kind of configuration debt that tends to surface later—often when it&#39;s least convenient.</p>',41)]))}const f=a(n,[["render",o]]);export{u as __pageData,f as default};
